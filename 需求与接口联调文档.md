# 「咱俩练练」活动页 · 需求与接口联调文档

> 面向后端 / 平台技术同事。一页看懂活动、页面逻辑和需要联调的接口。
> 更新日期:2026-09-22
> **内网地址(同事优先用):http://192.168.0.105:4318/**(公司 Wi-Fi 直接打开,挂在 Molly 公司电脑上)
> 线上演示地址:https://molly1225.github.io/oopz-duel/(纯静态演示,数据为本地模拟)
> 代码仓库:https://github.com/molly1225/oopz-duel

## 1. 活动简介

| 项目 | 内容 |
| --- | --- |
| 活动名 | 咱俩练练(O洲杯擂台) |
| 主办方 | Oopz 三角洲行动域(域号 330330) |
| 活动时间 | 2026.09.24 – 2026.10.31 |
| 形式 | 三角洲行动 1v1 单挑,玩家自发约战 |
| 奖励 | ① 域内指定活动频道开黑,得限定铭牌「"SOLO王"」15 天(平台侧发放);② 保存战绩认证书发布到抖音,得 7 天无限公民会员(纯运营发放,无开发量) |
| 域入口 | https://oopz.cn/i/TakELZ |

## 2. 核心玩法(用户视角)

1. **下战书**:填大名 + 一句狠话 → 生成带域二维码的战书海报(900×1250 PNG)→ 保存发给好友。
2. **上场练练**:好友扫码进 Oopz 域,双方在活动频道开黑单挑。
3. **战绩留名**:一方在活动页填战绩(大名、比赛时间、比分、留言 + 2 张凭证截图)→ 把邀请链接发给对手 → 对手填大名、留言并确认比分 → 生成双方认证书(900×1280 PNG)。
4. **冲榜**:双方确认后计分,进「O洲杯 擂台排行榜」。

## 3. 业务规则(需后端实现)

- 比分:0–999 整数;确认前发起方可改,确认后锁定。
- 凭证:发起方必填 2 张图(① 域内开黑截图需带设备时间戳;② 游戏结算页需含对局时间和比分)。JPG/PNG/WebP,单张 ≤ 9.5MB。
- 计分:双方确认后各得 1 分,胜方再得 1 分;平局各 1 分。**同一对手、同一比赛日期只计最先确认的一场**,其余对局仍可生成认证书但不计分。
- 榜单:积分降序,同分按胜场降序。**最多展示 50 条**;前端容器默认可见 7 行,超出容器内滚动。
- 大名/留言留空时:取用户昵称 / 随机一句留言(前端已实现,后端入库前字段以最终值为准)。
- 比赛时间:前端默认当前时刻,**不限制在活动日期范围内**;时间以北京时间为准。异常战绩(重复、改分等)处理流程需后端定。

## 4. 需要联调的接口

当前前端为纯静态演示:身份、战绩、榜单均为本地模拟(localStorage 键 `oopz-duel-demo-v1`)。上线需要以下接口,建议统一鉴权(Oopz 登录态):

| # | 接口 | 说明 | 前端替换点 |
| --- | --- | --- | --- |
| 1 | **获取当前用户** `GET /api/me` | 返回 `uid / pid / vid / 昵称 / 头像`。好友打开邀请链接时同样取 ta 的身份 | `dist/app.js` 顶部 `ME`、`FRIEND` 两个写死的演示身份 |
| 2 | **创建/更新战绩** `POST /api/records`(`PUT /api/records/:id`) | 字段:发起方 uid、大名、比赛时间、双方比分、留言、凭证图 2 张(建议先走 #5 上传后传 URL)。确认前可改 | `recordForm()` 提交逻辑,替换 `persist()` 本地保存 |
| 3 | **查询战绩** `GET /api/records/:id` | 邀请链接只带记录 id(`?record=xx&view=friend`),好友打开时拉取 | `friendForm()` 目前只查本机 localStorage,跨设备必须走服务端 |
| 4 | **确认战绩** `POST /api/records/:id/confirm` | 接受方 uid + 大名 + 留言;服务端校验:只有受邀对手可确认、确认后锁定、防并发/防篡改 | `friendForm()` 的 onsubmit |
| 5 | **上传凭证图** `POST /api/upload` |  multipart,返回 URL;限制 JPG/PNG/WebP ≤ 9.5MB。当前前端是 base64 存 localStorage,正式环境必须改 | `recordForm()` 的图片处理 |
| 6 | **排行榜** `GET /api/leaderboard` | 按第 3 节规则服务端计分;返回排名、昵称、头像、计分场次、胜场、积分 | `render()` 榜单部分,替换预置示例数据 |

> 邀请链接格式已由前端定好:`{页面地址}?record={id}&view=friend`,后端只需按 id 提供查询。

## 5. 已完成、无需重复做的部分

- **飞书多维表格同步**:战绩确认后自动写入运营表格(静默,用户无感知,前端不出现相关文案)。已实现于 `server.js` 的 `POST /api/feishu`,配置在 `feishu-config.json`(已配通并实测,不上仓库)。表格字段:`UID / PID / VID / 用户昵称 / 接受者用户昵称 / 接受者UID / 接受者PID / 接受者VID / 比赛时间 / 发起方比分 / 对手比分 / 提交时间`。后端正式上线后可直接接管该逻辑,或保留此中转。
- **海报生成**:战书 / 认证书均为前端 Canvas 本地生成,不调用任何模型或服务端接口。移动端保存走系统分享面板,可直接存入相册。
- **前端全部交互**:表单校验、随机狠话/留言、凭证示例图 hover/点击、飘红提示、toast(页面居中、0.8s 自动消失)等已定稿。静态资源带 `?v=` 版本号防缓存。

## 6. 联调依赖与排期建议

1. 接口 1(用户身份)最优先 —— 前端所有 uid 字段都等它;
2. 接口 2/3/4(战绩链路)第二批 —— 决定邀请链接能否跨设备;
3. 接口 5/6 随后;
4. 铭牌由平台侧发放、抖音会员由运营发放,均无接口依赖,页面只展示权益文案。

前端联调联系人:活动页由 Kimi 托管维护,改前端直接说需求即可。

## 7. 本地运行

```sh
cd 咱俩练练活动页
npm run dev -- --port 5173   # 零依赖,Node ≥ 18
# 打开 http://localhost:5173
```

`dist/` 即全部前端源码(原生 HTML/JS,无构建)。线上部署:静态托管 `dist/` 即可;如需飞书同步则同机跑 `server.js`(含 `/api/feishu`)。
