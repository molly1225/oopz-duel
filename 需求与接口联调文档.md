# 「咱俩练练」活动页 · 需求与接口联调文档

> 面向后端 / 平台技术同事。一页看懂活动、页面逻辑和需要联调的接口。
> 更新日期:2026-09-23
> **内网地址(同事优先用):http://192.168.0.105:4318/**(公司 Wi-Fi 直接打开,挂在 Molly 公司电脑上)
> 线上演示地址:https://molly1225.github.io/oopz-duel/(纯静态演示,数据为本地模拟)
> 代码仓库:https://github.com/molly1225/oopz-duel

## 1. 活动简介

| 项目 | 内容 |
| --- | --- |
| 活动名 | 咱俩练练(O洲杯擂台) |
| 主办方 | Oopz 三角洲行动域(域号 330330) |
| 活动时间 | 2026.09.24 – 2026.10.31 |
| 形式 | 三角洲行动 1v1 单挑,玩家自发约战 |
| 奖励 | ① 域内指定活动频道开黑,得限定铭牌「"SOLO王"」15 天(平台侧发放);② 保存战绩认证书发布到抖音,得 7 天无限公民会员(纯运营发放,无开发量) |
| 域入口 | https://oopz.cn/i/TakELZ |

## 2. 核心玩法(用户视角)

1. **下战书**:填大名 + 一句狠话 → 生成带域二维码的战书海报(900×1250 PNG)→ 保存发给好友。
2. **上场练练**:好友扫码进 Oopz 域,双方在活动频道开黑单挑。
3. **战绩留名**:一方在活动页填战绩(大名、比赛时间、比分、留言 + 2 张凭证截图)→ 把邀请链接发给对手 → 对手填大名、留言并确认比分 → 生成双方认证书(900×1280 PNG)。
4. **冲榜**:双方确认后计分,进「O洲杯 擂台排行榜」。

## 3. 业务规则

- 比分:0–999 整数;确认前发起方可改,确认后锁定(服务端已拦截重复确认和确认后修改)。
- 凭证:发起方必填 2 张图(① 域内开黑截图需带设备时间戳;② 游戏结算页需含对局时间和比分)。JPG/PNG/WebP,单张 ≤ 9.5MB。
- 计分:双方确认后各得 1 分,胜方再得 1 分;平局各 1 分。**同一对手、同一比赛日期只计最先确认的一场**,其余对局仍可生成认证书但不计分。**未确认的战绩不计分**。
- 榜单:积分降序,同分按胜场降序。**最多展示 50 条**;前端容器默认可见 7 行,超出容器内滚动。
- 大名/留言留空时:取用户昵称 / 随机一句留言(前端已实现,后端入库前字段以最终值为准)。
- 比赛时间:前端默认当前时刻,**不限制在活动日期范围内**;时间以北京时间为准。异常战绩(重复、改分等)处理流程需后端定。

## 4. 后端现状:飞书表即数据库,技术侧只剩身份接口

战绩链路、凭证图、排行榜已全部实现于 `server.js`(已端到端实测),**飞书多维表格直接当数据库用,不落任何库**。接口都可直接保留作正式版,也可由后端重写替换:

| # | 接口 | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | **获取当前用户** `GET /api/me` | **待接(唯一待办)** | Oopz 现成接口。返回 `uid / pid / vid / 昵称 / 头像`。前端替换点:`dist/app.js` 顶部 `ME`、`FRIEND` 两个写死的演示身份 |
| 2 | 创建/更新战绩 `POST /api/records` | ✅ 已实现 | 以「记录ID」为键:不存在则创建(状态=待确认),存在且未确认则更新,已确认拒绝。含两张凭证图上传(转飞书附件字段) |
| 3 | 查询战绩 `GET /api/records?id=` | ✅ 已实现 | 邀请链接 `?record={id}&view=friend` 跨设备打开即查 |
| 4 | 确认战绩 `POST /api/records/confirm` | ✅ 已实现 | 写接受者大名/留言、状态→已确认、提交时间;重复确认返回 `already_confirmed` |
| 5 | 凭证图读取 `GET /api/media/:token` | ✅ 已实现 | 飞书素材需鉴权,经此代理给前端展示 |
| 6 | 排行榜 `GET /api/leaderboard` | ✅ 已实现 | 飞书表数据源,只计已确认;按第 3 节规则计分;30s 缓存,写战绩后失效 |

> **上线前唯一要认真做的一件事:身份校验**。目前写接口无鉴权(内网无所谓,公网不行)。正式环境:前端拿 Oopz 登录态 → `server.js` 写飞书前调用接口 1 验证调用者,同时把真实 uid/pid/vid 填进表格。接口 1 文档(地址、入参、返回字段)麻烦提供给前端。

## 5. 飞书表结构

战绩确认链路全程写一张多维表格(运营可直接查看/修正,等于自带后台):

- 文本:`记录ID / UID / PID / VID / 用户昵称(发起方大名) / 接受者用户昵称 / 接受者UID / 接受者PID / 接受者VID / 发起方留言 / 接受者留言 / 状态(待确认|已确认)`
- 日期(ms):`比赛时间 / 提交时间`
- 数字:`发起方比分 / 对手比分`
- 附件:`开黑图 / 游戏结算图`

字段不存在时服务端会自动创建;各类 ID 暂留空,等接口 1 接入。

## 6. 前端已完成、无需重复做的部分

- **海报生成**:战书 / 认证书均为前端 Canvas 本地生成,不调用任何模型或服务端接口。移动端保存走系统分享面板,可直接存入相册。
- **全部交互**:表单校验、随机狠话/留言、凭证示例图(PC hover / 移动端点击)、飘红提示、toast(页面居中、0.8s 自动消失)等已定稿。静态资源带 `?v=` 版本号防缓存。
- 无服务端时(GitHub Pages 纯静态)自动回退 localStorage 演示模式,页面不报错。

## 7. 本地运行

```sh
cd 咱俩练练活动页
npm run dev -- --port 5173   # 零依赖,Node ≥ 18
# 打开 http://localhost:5173
```

`dist/` 即全部前端源码(原生 HTML/JS,无构建)。上线:静态托管 `dist/` + 同机跑 `server.js`(战绩/榜单/凭证图接口)。纯静态托管也能跑,但退化为单机演示(链接跨设备不可用)。
